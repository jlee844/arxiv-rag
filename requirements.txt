# Fetching
arxiv==2.1.3
requests==2.32.3

# PDF parsing
pymupdf==1.24.11

# Embeddings (CPU-only, no GPU needed)
sentence-transformers==3.3.1

# Vector store
chromadb==0.5.20

# BM25 (pure Python)
rank-bm25==0.2.2

# Snowball stemming for the BM25 tokenizer. Measured: +1.03pp recall@5 and
# +0.007 MRR over `text.lower().split()` — the whole of LlamaIndex's apparent
# retrieval advantage in the E1 ablation (see EVAL.md).
PyStemmer==3.1.0

# Generation — Ollama (local, no GPU required on Apple Silicon via Metal)
ollama==0.4.4

# OpenAI fallback (optional, set OPENAI_API_KEY)
openai==1.55.3

# CLI
click==8.1.8

# Utilities
tqdm==4.67.1
python-dotenv==1.0.1

# API
fastapi==0.140.13
uvicorn==0.51.0

# MCP server (exposes retrieval to any MCP client)
mcp==2.0.0
