# Fetching
arxiv==2.1.3
requests==2.32.3

# PDF parsing
pymupdf==1.24.11

# Embeddings (CPU-only, no GPU needed)
sentence-transformers==3.3.1

# Vector store
chromadb==0.5.20

# BM25 (pure Python)
rank-bm25==0.2.2

# Generation — Ollama (local, no GPU required on Apple Silicon via Metal)
ollama==0.4.4

# OpenAI fallback (optional, set OPENAI_API_KEY)
openai==1.55.3

# CLI
click==8.1.8

# Utilities
tqdm==4.67.1
python-dotenv==1.0.1

# API
fastapi==0.140.13
uvicorn==0.51.0
